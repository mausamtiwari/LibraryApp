package be.intecbrussel.model.user;

public class Member extends User {
    public Member(String firstName, String lastName, String emailId, String password) {
        super(firstName, lastName, emailId, password);
    }
}
